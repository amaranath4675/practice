-name: playlevel_variables
hosts: local
connection: local
vars:
  COURSE: ansibile
  DURATION: 120hrs
tasks:
  -name: print the course details
  ansibile.builtin.debug:
  msg: "Course is {{COURSE}} and duration is {{DURATION}}"